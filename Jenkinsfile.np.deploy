pipeline {
    agent { label 'docker' }
    options {
        disableConcurrentBuilds()
    }
    stages {
        stage('Deploy') {
            steps {
                script {
                  echo "This is a deploy"
                }
            }
        }
    }
}
