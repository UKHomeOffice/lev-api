library(
        identifier: 'jenkins-shared@lev-65-trivy-for-cd',
        retriever: modernSCM([$class       : 'GitSCMSource',
                              remote       : 'ssh://git@bitbucket.ipttools.info/lev/jenkins-shared.git',
                              credentialsId: 'git'])
)

pipelineDeployment {
    environments = ['dev1']
    application = 'lev-api'
    service = 'api'
    zone = 'i'
    dockerImagePath = 'ho/hmpo/lev/api'
}
